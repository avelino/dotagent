# MOVED — the canonical Homebrew formula now lives at:
#
#   Formula/dotagent.rb
#
# This file is kept as a stub so old links don't 404. Delete in a
# future cleanup pass.
#
# Why the move: the repo doubles as its own Homebrew tap, and `brew
# tap` looks for formulae under `Formula/` at the repo root. See
# `scripts/homebrew/README.md` for the full story.
